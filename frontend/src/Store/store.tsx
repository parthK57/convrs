import { configureStore } from "@reduxjs/toolkit";

// TODO: Create logged in userdetails slice, 
// TODO: Current profile slice, selected message slice
// TODO: group chat mode slice, all friends slice

export const store = configureStore({});
